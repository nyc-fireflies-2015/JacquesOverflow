<%= form_for @comment, url: answer_comments_path(answer), html: {id: "answer-comment-form"} do |f| %>
	<%= text_area(:comment, :content, cols: 30, rows: 5) %><br>
	<%= f.submit "Add comment" %>
<% end %>